using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class dowyjebania : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("dupa");
        Debug.Log("kupa");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
